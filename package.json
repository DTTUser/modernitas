{
  "name": "modernitas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:demo": "node build.mjs --demo",
    "build:live": "node build.mjs --live",
    "dev": "node build.mjs --demo && python3 -m http.server 8080 --directory dist",
    "verify": "node ../verify.mjs"
  },
  "dependencies": {
    "@fontsource/public-sans": "^5.3.0",
    "marked": "^16.0.0"
  }
}
